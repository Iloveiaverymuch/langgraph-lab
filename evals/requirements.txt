# Eval harness deps. langchain-core (message types + token callback) is already a
# top-level repo dep; pinned here too so the evals/ dir is self-describing.
langchain-core>=0.3.0
# LLM-as-judge runs on Claude Haiku.
anthropic>=0.40.0
