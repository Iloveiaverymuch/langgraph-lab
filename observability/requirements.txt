# Sentinel observability deps.
# Export path: raw OpenTelemetry -> Langfuse OTLP endpoint. No langfuse SDK needed
# for tracing (it comes back only for the replay/query step, task #6).
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
